var car = {
    speed :"60km/h",
    name:"Lamba",
    color:"red",
    toString:function(){
   return `name: ${this.name}; speed: ${this.speed}; color: ${this.red}`
  }
  };
  console.log(car.speed);
  console.log(car.name);
  console.log(car.color);
  console.log(car.toString());